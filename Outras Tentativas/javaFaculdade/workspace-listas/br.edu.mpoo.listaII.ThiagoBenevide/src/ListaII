Lista 2

1) Ambiente de programaçaõ é algum muito ambrangente pois envolve a necessidade de várias caracteristicas que o um dispositivo como Notebook ou PC
possui além do ambiente em si inde se encontra o programador, software e hadware.

2) Editor de Texto, Linker, Depurador, Terminal, Compilador

3) Significar verificar e identificar problemas a fim de solucioná-los

4) Da JVM, Compilador Java e APIs

5) JDK é o ambiente de desenvolvimento java, já o JRE são arquivos que fazem apenas o java funcionar em um PC

6)Converter os bytecodides para fazer o software funcionar independentemente do sistema operacional que esteja sendo utilizado pelo usuário

7) É composta por visibilidade, retorno, nome e parâmetros

8) Quando se coloca um atributo ou método como static ele passa a ser um atributo ou método de cada objeto e sim da classe.
 
9) O this possui várias funções entre elas, para referênciar um atributo fora do método

10) Objeto é uma entidade que possui estado que representa o valor do objeto, comportamento que representa a funcionalidade do obejto e identide,
que é algo que não fica visível para o usuário apenas para JVM

Quando um objeto é criado porém sem nome, sigifica que ele é anonimo, porém só pode existir um dele

11.1) Ele sempre terá o mesmo nome da classe onde se encontra além de não ter retorno
11.2) Não pois o java em si criar automaticamente o construtor padrão em parâmetro algum, porém caso necessário deve colocar o valor
11.3) Não
11.4) É um método usado para liberar memória do sistema 
11.5) Serve para execultar ações de limpeza antes que o método seja descartado.

12)a) Não, porque o gc acontece em uma variável de parâmetro do  método e não no atributo principal
b) Sim, pois utilizando o this daria para cessar o atributo principal

13)a) Após a identificação de objetos que não estão sendo inicializado, o gc é feito automaticamente pelo java
b)
