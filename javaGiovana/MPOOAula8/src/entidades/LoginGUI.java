ackage entidades;

import javax.swing.JFrame;

public class LoginGUI extends JFrame {

	// try catch é um tratamento de exceção
	// criar uma classe para o botao e fazer a validação, actionPerfomed
	// uma classe interna de uma classe abstrata no contexto de base estática, temos que colocar a classe interna como estática tbm
	// colocar na tela, porem com setVisible false;
	// colocar o action em todos os campos
	// usar o e.Source para puxar a classe da tela
	// usar o isSelected para verficiar se tá selecionado
	// usar o instanceof para diferenciar quando a subclasse herdar de uma classe abstrata
	
}
